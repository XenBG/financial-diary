/** @type {import('tailwindcss').Config} */
export default {
    content: ['./src/**/*.{html,js,vue}'],
    darkMode: 'class',
    theme: {
        fontFamily: {
            sans: ['Sofia Sans'],
            serif: ['serif'],
            mono: ['monospace'],
            display: ['JetBrains Mono'],
            body: ['Sofia Sans'],
        },
    },
}
